import { Router } from "express";
import { authorizePilotFunding,getPilotFundingDashboard,prepareResidualFormula,recordApprovedProjectCost,recordRevenuePeriod } from "../companyFoundry/pilotFunding.js";
export const pilotFundingRouter=Router();
pilotFundingRouter.post("/foundry/pilot-funding",(req,res)=>{const r=authorizePilotFunding({applicationId:String(req.body?.applicationId??""),authorizedBy:String(req.body?.authorizedBy??""),approvedBudgetUsd:Number(req.body?.approvedBudgetUsd),rationale:String(req.body?.rationale??"")});return r.ok?res.status(201).json(r):res.status(r.error==="self_funding_authorization_forbidden"?403:409).json(r);});
pilotFundingRouter.post("/foundry/pilot-funding/:id/costs",(req,res)=>{const r=recordApprovedProjectCost({authorizationId:req.params.id,amountUsd:Number(req.body?.amountUsd),reference:String(req.body?.reference??""),recordedBy:String(req.body?.recordedBy??"")});return r.ok?res.status(201).json(r):res.status(409).json(r);});
pilotFundingRouter.post("/foundry/pilot-funding/:id/revenue-periods",(req,res)=>{const r=recordRevenuePeriod({authorizationId:req.params.id,periodKey:String(req.body?.periodKey??""),eligibleRevenueUsd:Number(req.body?.eligibleRevenueUsd),recordedBy:String(req.body?.recordedBy??"")});return r.ok?res.status(201).json(r):res.status(409).json(r);});
pilotFundingRouter.put("/foundry/pilot-funding/:id/residual-formula",(req,res)=>{const r=prepareResidualFormula({authorizationId:req.params.id,companyPct:Number(req.body?.companyPct),leadPct:Number(req.body?.leadPct),teamPct:Number(req.body?.teamPct),approvedBy:String(req.body?.approvedBy??"")});return r.ok?res.json(r):res.status(409).json(r);});
pilotFundingRouter.get("/foundry/pilot-funding/:id",(req,res)=>{const d=getPilotFundingDashboard(req.params.id);return d?res.json(d):res.status(404).json({error:"pilot_funding_not_found"});});
