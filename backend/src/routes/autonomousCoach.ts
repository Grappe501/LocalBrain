import {Router} from "express";import {defineTerm,getAutonomousCoach,getLessonExperience} from "../companyFoundry/autonomousCoach.js";import {buildCurriculumCatalogue,buildLesson} from "../companyFoundry/lessonFactory.js";import {askGroundedTutor} from "../companyFoundry/groundedTutor.js";export const autonomousCoachRouter=Router();
autonomousCoachRouter.get("/foundry/academy/enrollments/:id/experience",(req,res)=>{const d=getAutonomousCoach(req.params.id);return d?res.json(d):res.status(404).json({error:"academy_enrollment_not_found"});});
autonomousCoachRouter.get("/foundry/academy/lessons",(_req,res)=>res.json({lessons:buildCurriculumCatalogue()}));
autonomousCoachRouter.get("/foundry/academy/lessons/:moduleId/experience",(req,res)=>{const base=getLessonExperience(req.params.moduleId);const content=buildLesson(req.params.moduleId);return base&&content?res.json({...base,content}):res.status(404).json({error:"academy_module_not_found"});});
autonomousCoachRouter.get("/foundry/academy/glossary/:term",(req,res)=>{const d=defineTerm(req.params.term);return d?res.json(d):res.status(404).json({error:"term_not_found"});});
autonomousCoachRouter.post("/foundry/academy/enrollments/:id/tutor",async(req,res)=>{const question=String(req.body?.question??"").trim();if(!question)return res.status(400).json({error:"question_required"});const result=await askGroundedTutor({enrollmentId:req.params.id,question});if(result.ok)return res.json(result);const status=result.error==="academy_enrollment_not_found"?404:result.error==="no_ai_provider_configured"?503:502;return res.status(status).json(result);});
