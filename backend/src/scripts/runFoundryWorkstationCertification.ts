import {spawnSync} from 'node:child_process';import {writeFileSync,mkdirSync} from 'node:fs';import {resolve} from 'node:path';
type R={id:string;label:string;command:string;status:'pass'|'fail'|'warn';detail:string;durationMs:number};
const root=resolve(process.cwd());const checks:[string,string,string[]][]=[['typecheck','TypeScript typecheck','npm',['run','typecheck']],['build','Production build','npm',['run','build']],['backend-tests','Backend test suite','npm',['run','test']],['academy-smoke','Academy source smoke','node',['--input-type=module','-e',`import('./backend/dist/companyFoundry/academyCurriculum.js').then(m=>{if(!m.academyModules?.length)process.exit(1);console.log('modules',m.academyModules.length)})`]]];
const results:R[]=[];for(const [id,label,cmd,args] of checks){const started=Date.now();const r=spawnSync(cmd,args,{cwd:root,encoding:'utf8',shell:process.platform==='win32'});const detail=[r.stdout,r.stderr].filter(Boolean).join('\n').slice(-12000);results.push({id,label,command:[cmd,...args].join(' '),status:r.status===0?'pass':'fail',detail:detail||`exit ${r.status}`,durationMs:Date.now()-started});}
const packet={schemaVersion:'1.0',label:'CF-014 Workstation Certification',createdAt:new Date().toISOString(),machine:{platform:process.platform,node:process.version,cwd:root},checks:results,summary:{pass:results.filter(x=>x.status==='pass').length,fail:results.filter(x=>x.status==='fail').length,warn:results.filter(x=>x.status==='warn').length,score:Math.round(results.filter(x=>x.status==='pass').length/results.length*100)}};mkdirSync(resolve(root,'reports'),{recursive:true});const out=resolve(root,'reports','CF_014_WORKSTATION_CERTIFICATION.json');writeFileSync(out,JSON.stringify(packet,null,2));console.log(JSON.stringify(packet.summary));console.log(`Evidence: ${out}`);process.exit(packet.summary.fail?1:0);
